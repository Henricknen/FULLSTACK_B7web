let numero = 0;

while(numero <= 10) {       // 'while' enquanto o número for menor ou igual à 10 será executado o loop
    console.log(`Esse código está sendo executado ${numero} vezes`);
    numero++;       // por variável número ser igual á 0, para evitar loop infinito 'numero++' incrementa 1 a cada loop executado
}