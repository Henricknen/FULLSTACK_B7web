function somar(n1: number, n2: number, n3?: number): number {       // '?' indica que o parâmetro é opcional
    let nome: string = 'Luis Henrique S F';     // variável 'nome' declarada mais nunca utilizada
    return n1 + n2;
}

somar(10, 78);